# Universal Booking Platform

## Modules

- [UBP.IAM](UBP.IAM/README.MD) — Identity & Access Management (OAuth 2.0 / OpenID Connect)

## Database Migrations

### Prerequisites

Install the EF Core CLI tools (once per machine):

```powershell
dotnet tool install --global dotnet-ef
```

See each module's README for migration commands specific to that module.
